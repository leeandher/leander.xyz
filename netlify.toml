[build]
base = ""
publish = "public"
command = "npm run build"

[build.environment]
NODE_OPTIONS = "--max_old_space_size=4096 --openssl-legacy-provider"

[context.dev]
base = ""
publish = "public"
command = "npm run build"
